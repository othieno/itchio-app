language: cpp
compiler:
   - gcc
#   - clang
before_install:
   - sudo add-apt-repository --yes ppa:ubuntu-sdk-team/ppa
   - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
   - sudo apt-get update -qq
install:
   - sudo apt-get install -qq qt5-qmake qt5-default g++-4.9
   - export CXX="g++-4.9"
before_script:
   - qmake -qt=5
script:
   - make
branches:
   only:
      - experimental
os:
   - linux
